{
  "mcpServers": {
    "serena": {
      "command": "uv",
      "args": ["run", "serena", "start-mcp-server", "--project", "${workspaceFolder}"],
      "cwd": "${workspaceFolder}",
      "env": {
        "SERENA_LOG_LEVEL": "info"
      }
    }
  }
}
